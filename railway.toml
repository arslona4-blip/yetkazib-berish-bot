# Vositachi — Railway worker (long polling). Bir xizmat: python -m vositachi
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m vositachi"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
